# Moodle submission checklist (SD1)

You must submit:
1) **HTTPS link** to your GitHub repository (example):
   `https://github.com/your_user/sd1-conference-system`
2) **ZIP** archive of your project.

Tip: when zipping, exclude `vendor/` and `node_modules/` (they are huge).
